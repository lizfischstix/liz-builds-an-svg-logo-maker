class Logo{
    constructor(shape, backgroundColor,textColor, monogram){
        this.backgroundColor = backgroundColor;
        this.textColor = textColor;
        this.monogram = monogram
        this.shape = shape;
    }
}

// class Shape extends Logo{
//     constructor(backgroundColor, textColor, monogram){
//         super(shape, backgroundColor, textColor, monogram)
// }
// }

const shape = new userInput.shape (userInput.backgroundColor, userInput.textColor, userInput.monogram)

